// import { GreyCat } from '@greycat/web';
// import '@/common';

// const greycat = await await gc.sdk.init();

// const evolutions = greycat.evolutions();
// console.log(evolutions.map((e) => e.head.data.name));

// document.body.appendChild(
//   <app-layout title="Abi" mainClassName="list">
//     <div className="list">
//       <div>Found {evolutions.length} evolution(s):</div>
//       {evolutions.map((e) => (
//         <gui-abi-type-evol value={e} />
//       ))}
//     </div>
//   </app-layout>,
// );
